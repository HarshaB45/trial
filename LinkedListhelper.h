struct node {
    //Data input into the node
    int data;
    //A pointer to point to the next element
    struct node *next;
};
void createLinkedList(struct node *head)
void LinkedlistTraversal(struct node *head, struct node *temp)
